# NPM configuration for the cerebellum project

# Specify the location of build files
dist-dir=dist

# Ensure that the package-lock.json file is always generated
package-lock=true

# Use a more strict versioning strategy
save-exact=true

# Increase the network timeout for slow connections
network-timeout=60000

# Set the default access level for published packages (if applicable)
access=public

# Disable automatic running of scripts for security
ignore-scripts=true

# Add any other project-specific configurations below
